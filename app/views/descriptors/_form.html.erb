<%= form_for(@descriptor) do |f| %>
  <% if @descriptor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@descriptor.errors.count, "error") %> prohibited this descriptor from being saved:</h2>

      <ul>
      <% @descriptor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
