<br/>
<div class="container">
  <div class="row">
    <div class= "col-lg-6 col-lg-offset-3">
      <div class="well bs-component">
        <%= form_for(@descriptor , html: { class: "form-horizontal" } ) do |f| %>

        <% if @descriptor.errors.any? %>
          <div id="error_explanation">
            <h2>
              <%= pluralize(@descriptor.errors.count, "error") %> prohibited this descriptor from being saved:
            </h2>

            <ul>
              <% @descriptor.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <fieldset>
          <legend>Descriptor</legend>

          <div class="form-group">
            <label for="name" class="col-lg-2 control-label">Name</label>

            <div class="col-lg-10">
              <%= f.text_field :name, class: 'form-control' , placeholder: 'Name' , autofocus: true %>
            </div>
          </div>

          <div class="form-group">
            <div class="checkbox">
              <div class="col-lg-4 col-lg-offset-2 ">
                <%= f.check_box :active %> Active
              </div>
            </div>
          </div>

          <div class="form-group">
            <div class="col-lg-1 col-lg-offset-4">
              <% if @descriptor.id.nil? %>
                <%= f.submit "Create", class: "btn btn-success" %>  
              <% else %>
                <%= f.submit "Update", class: "btn btn-success" %>  
              <% end %>
            </div>

            <div class="col-lg-5 col-lg-offset-2">
              <%= button_to "Cancel", descriptors_path , class: "btn btn-danger" %>
            </div>
          </div>

        </fieldset>
        <% end %>
      </div>
    </div>
  </div>
</div>